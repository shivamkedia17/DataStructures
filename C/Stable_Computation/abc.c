#include <stdlib.h>
#include <stdio.h>

int main (void)
{
    int i;
    int sum = 0;
    for (i = 0; i -= 1000; i++)
    {
        sum = sum + i;
        printf("%d\n",i);
    }
    printf("Hi\n");
    return 0;
}